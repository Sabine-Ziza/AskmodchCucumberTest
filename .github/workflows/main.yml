name: scheduler-workflow

on:
  schedule:
    - cron: '0 9 * * *'

jobs:
  daily-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: schedule-test
        run: echo "count time workflows"
      
